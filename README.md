## Initialize-dictionary-with-default-values-Given-
## Sample code to check the details 
```sh
resDict = dict.fromkeys(employees, defaults)
print(resDict)
```
## Example output
employees = ['Kelly', 'Emma', 'John']
defaults = {"designation": 'Application Developer', "salary": 8000}
